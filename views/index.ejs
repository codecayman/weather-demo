<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Weather.Code(Cayman)</title>
    <link rel="stylesheet" type="text/css" href="/css/weather.css">
    <link href='https://fonts.googleapis.com/css?family=Open+Sans:300' rel='stylesheet' type='text/css'>
  </head>
  <body>
    <div class="container">
        <img src="/images/logo.png" />
      <fieldset>
        <form action="/" method="post">
          <input name="city" type="text" class="weather-input" placeholder="Enter City" required>
          <input type="submit" class="weather-button" value="Get Weather">
        </form>
      </fieldset>
      <% if(weather !== null){ %>
        <p><%= weather %></p>
      <% } %>

      <% if(error !== null){ %>
        <p><%= error %></p>
      <% } %>
    </div>
  </body>
</html>